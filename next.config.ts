import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow all external image sources
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
      {
        protocol: 'http',
        hostname: '**',
      },
    ],
  },
};

export default nextConfig;
